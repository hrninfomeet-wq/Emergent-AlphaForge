# Web framework + server
fastapi==0.110.1
uvicorn==0.25.0
pydantic>=2.6.4
python-dotenv>=1.0.1
tzdata>=2024.2

# Database
pymongo==4.5.0
motor==3.3.1

# HTTP / broker connectivity (Upstox data, Flattrade OMS)
requests>=2.31.0
httpx>=0.27.0
websockets>=15.0.0
cryptography>=42.0.8

# Data + research engines
pandas>=2.2.0
numpy>=1.26.0
yfinance>=0.2.66
optuna==4.8.0
cmaes==0.13.0
# optuna's param-importance (optimizer.py _param_importance -> FanovaImportanceEvaluator)
# imports sklearn lazily at call time — removing this silently blanks that panel.
scikit-learn==1.8.0

# AI strategy authoring
anthropic>=0.77,<1.0
google-genai>=1.0,<2.0
youtube-transcript-api>=1.1,<2.0

# Tests (the documented workflow runs pytest inside the backend container)
pytest>=8.0.0
pytest-asyncio>=0.23
